pagbank:
  token: your-sandbox-token-here  # Set your PagBank sandbox API token here before running
  environment: SANDBOX
  log-requests: true

spring:
  jackson:
    default-property-inclusion: non_null

logging:
  level:
    io.github.rodrigoma.pagbank: DEBUG
